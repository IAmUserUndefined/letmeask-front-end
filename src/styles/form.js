import styled from "styled-components";

const Form = styled.form`
    width: 100%;
    padding: 1rem;
    text-align: center;

    > div, h2 {
        margin-bottom: 1rem;
    }
`;

export default Form;